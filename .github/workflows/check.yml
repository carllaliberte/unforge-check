name: check
# L'état désiré est le workflow `constat`.
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  pointer:
    runs-on: ubuntu-latest
    steps:
      - run: echo "voir .github/workflows/constat.yml"
